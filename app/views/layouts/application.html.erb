<!DOCTYPE html>
<html>
  <head>
    <title>WarehouseApp - Feito na Campus Code </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <%= link_to root_path do %>
        <h1 class="header">  Sistema de Galpões & Estoque </h1>
      <% end %>
      
    </header>
    <nav>
          
          
      <%= link_to 'Fornecedores', suppliers_path, class: "botao" %>
      <%= link_to 'Modelos de Produtos', product_models_path, class: "botao" %>
      <%= link_to 'Registrar Pedido', new_order_path %>
      <% if user_signed_in? %>
        <%= button_to 'Sair', destroy_user_session_path, class: "login", method: :delete %>
        <p class="login"><%= current_user.description %></p>
        
      <% else %>
        <%= link_to 'Entrar', new_user_session_url, class: "login" %>
      <% end %>
    </nav>
    <div>
      <%= notice  %>
      <%= alert %>
    </div>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
