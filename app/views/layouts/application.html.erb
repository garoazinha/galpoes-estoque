<!DOCTYPE html>
<html>
  <head>
    <title>WarehouseApp - Feito na Campus Code </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <h1 class="header"> Sistema de Galpões & Estoque </h1>
      
    </header>
    <nav>
          <%= link_to 'Home', root_path, class: "botao" %>
          
          <%= link_to 'Fornecedores', suppliers_path, class: "botao" %>
          <%= link_to 'Modelos de Produtos', product_models_path, class: "botao" %>
          <% if user_signed_in? %>
            <%= current_user.email %>
            <%= link_to 'Sair', root_path, class: "botao" %>
          <% else %>
            <%= link_to 'Entrar', new_user_session_url, class: "botao" %>
          <% end %>
    </nav>
    <div>
      <%= notice  %>
      <%= alert %>
    </div>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
